# PostgreSQL connection settings (local database)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cinevault
DB_USER=postgres
DB_PASSWORD=scv1ctcv7u

# Server settings
PORT=5000
NODE_ENV=development

# Frontend origin allowed to call the API (CORS)
CLIENT_ORIGIN=http://127.0.0.1:5500
